"use client";

// Temporarily disabled on the public website because the external chat widget can block page clicks.
// Keep this component in place so the widget can be safely re-enabled later without changing layout.tsx.
export function ServiceAiChatWidget() {
  return null;
}
