# Calendar resource view

This phase adds a staff-column view to the business calendar.

- one column per known staff member
- unassigned bookings in a dedicated column
- time off shown in the matching staff column
- week navigation reuses the existing calendar cursor
- no database migration

The next phase adds controlled drag-and-drop movement with server-side validation.
