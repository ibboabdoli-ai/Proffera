# P100 — Dashboard stat clarity

## Outcome

The dashboard now labels totals and status subsets separately. For example, a workspace with 11 saved customers and 1 active customer displays both values explicitly instead of looking inconsistent.

## Scope

- Presentation-only dashboard copy change.
- No customer, booking, workspace, or AI data is modified.
